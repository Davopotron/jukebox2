Table User {
  id        Int     [pk, increment]
  username  String
  playlists Playlist[]
}

Table Playlist {
  id            Int     [pk, increment]
  name          String
  description   String
  owner         User
  tracks        Track[]
}

Table Track {
  id        Int [pk, increment]
  name      String
  playlists Playlist[]
}

//<: mouth opens toward the "many"

//Ref: User.playlists < Playlist.owner

//1 User has many Playlists
//the connecting fields are playlists and owner, respectively//

Ref: User.playlists < Playlist.owner
Ref: Playlist.tracks <> Track.playlists